#!/bin/sh
set -eu

swift test --parallel
swift run --skip-build swa unused Sources --mode reachability --sensible-defaults --format text
swift run --skip-build swa duplicates Sources --min-tokens 50 --format text
